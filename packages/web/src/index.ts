// web-client — Web Client
// Scope: Single-page browser application that renders a 9x9 Sudoku board, accepts player input, validates moves on the client, fetches a new puzzle from the Puzzle Service, and posts completed-game results to the Scores Service.
// Owns: packages/web
// This team builds its slice here each phase.
export {};
